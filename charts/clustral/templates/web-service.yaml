apiVersion: v1
kind: Service
metadata:
  name: {{ include "clustral.fullname" . }}-web
  labels:
    {{- include "clustral.labels" . | nindent 4 }}
    {{- include "clustral.selectorLabels" (dict "root" . "component" "web") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
  selector:
    {{- include "clustral.selectorLabels" (dict "root" . "component" "web") | nindent 4 }}

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.1
// - protoc             v7.34.1
// source: tunnel.proto

package clustralv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	TunnelService_OpenTunnel_FullMethodName = "/clustral.v1.TunnelService/OpenTunnel"
)

// TunnelServiceClient is the client API for TunnelService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// TunnelService is the core transport layer between the Agent and the
// ControlPlane.  The Agent opens a single persistent bidirectional stream
// (OpenTunnel) and the ControlPlane multiplexes individual kubectl HTTP
// requests over it using a lightweight framing protocol.
//
// Flow:
//  1. Agent connects and sends an initial AgentHello frame.
//  2. ControlPlane acknowledges with a TunnelHello frame.
//  3. For each proxied kubectl request the ControlPlane sends one or more
//     HttpRequestFrame messages identified by a request_id.
//  4. The Agent forwards the request to the in-cluster API server and streams
//     back one or more HttpResponseFrame messages with the same request_id.
//  5. Either side may send a PingFrame; the other side replies with a
//     PongFrame to keep the connection alive and detect half-open streams.
//  6. To cancel an in-flight request the ControlPlane sends a CancelFrame.
type TunnelServiceClient interface {
	// OpenTunnel is the single long-lived bidirectional stream per Agent.
	// The stream is authenticated via the agent credential in the gRPC metadata
	// (Authorization: Bearer <agent-token>).
	OpenTunnel(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[TunnelClientMessage, TunnelServerMessage], error)
}

type tunnelServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewTunnelServiceClient(cc grpc.ClientConnInterface) TunnelServiceClient {
	return &tunnelServiceClient{cc}
}

func (c *tunnelServiceClient) OpenTunnel(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[TunnelClientMessage, TunnelServerMessage], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &TunnelService_ServiceDesc.Streams[0], TunnelService_OpenTunnel_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[TunnelClientMessage, TunnelServerMessage]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type TunnelService_OpenTunnelClient = grpc.BidiStreamingClient[TunnelClientMessage, TunnelServerMessage]

// TunnelServiceServer is the server API for TunnelService service.
// All implementations must embed UnimplementedTunnelServiceServer
// for forward compatibility.
//
// TunnelService is the core transport layer between the Agent and the
// ControlPlane.  The Agent opens a single persistent bidirectional stream
// (OpenTunnel) and the ControlPlane multiplexes individual kubectl HTTP
// requests over it using a lightweight framing protocol.
//
// Flow:
//  1. Agent connects and sends an initial AgentHello frame.
//  2. ControlPlane acknowledges with a TunnelHello frame.
//  3. For each proxied kubectl request the ControlPlane sends one or more
//     HttpRequestFrame messages identified by a request_id.
//  4. The Agent forwards the request to the in-cluster API server and streams
//     back one or more HttpResponseFrame messages with the same request_id.
//  5. Either side may send a PingFrame; the other side replies with a
//     PongFrame to keep the connection alive and detect half-open streams.
//  6. To cancel an in-flight request the ControlPlane sends a CancelFrame.
type TunnelServiceServer interface {
	// OpenTunnel is the single long-lived bidirectional stream per Agent.
	// The stream is authenticated via the agent credential in the gRPC metadata
	// (Authorization: Bearer <agent-token>).
	OpenTunnel(grpc.BidiStreamingServer[TunnelClientMessage, TunnelServerMessage]) error
	mustEmbedUnimplementedTunnelServiceServer()
}

// UnimplementedTunnelServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedTunnelServiceServer struct{}

func (UnimplementedTunnelServiceServer) OpenTunnel(grpc.BidiStreamingServer[TunnelClientMessage, TunnelServerMessage]) error {
	return status.Error(codes.Unimplemented, "method OpenTunnel not implemented")
}
func (UnimplementedTunnelServiceServer) mustEmbedUnimplementedTunnelServiceServer() {}
func (UnimplementedTunnelServiceServer) testEmbeddedByValue()                       {}

// UnsafeTunnelServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to TunnelServiceServer will
// result in compilation errors.
type UnsafeTunnelServiceServer interface {
	mustEmbedUnimplementedTunnelServiceServer()
}

func RegisterTunnelServiceServer(s grpc.ServiceRegistrar, srv TunnelServiceServer) {
	// If the following call panics, it indicates UnimplementedTunnelServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&TunnelService_ServiceDesc, srv)
}

func _TunnelService_OpenTunnel_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(TunnelServiceServer).OpenTunnel(&grpc.GenericServerStream[TunnelClientMessage, TunnelServerMessage]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type TunnelService_OpenTunnelServer = grpc.BidiStreamingServer[TunnelClientMessage, TunnelServerMessage]

// TunnelService_ServiceDesc is the grpc.ServiceDesc for TunnelService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var TunnelService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "clustral.v1.TunnelService",
	HandlerType: (*TunnelServiceServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "OpenTunnel",
			Handler:       _TunnelService_OpenTunnel_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "tunnel.proto",
}

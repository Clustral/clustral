// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.1
// - protoc             v7.34.1
// source: tunnel_proxy.proto

package v1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	TunnelProxy_ProxyRequest_FullMethodName = "/clustral.internal.v1.TunnelProxy/ProxyRequest"
)

// TunnelProxyClient is the client API for TunnelProxy service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// TunnelProxy is an internal gRPC service that the stateless ControlPlane
// API calls to forward kubectl HTTP requests through a specific
// TunnelService pod that holds the agent's persistent tunnel stream.
//
// Flow:
//  1. ControlPlane receives a kubectl proxy request.
//  2. ControlPlane queries Redis for the tunnel pod that owns the
//     target cluster's session (e.g., "tunnel-0").
//  3. ControlPlane calls TunnelProxy.ProxyRequest on that pod's
//     internal gRPC endpoint (:50051).
//  4. TunnelProxy looks up the local TunnelSession, sends the HTTP
//     request frame to the Agent, waits for the response, returns it.
type TunnelProxyClient interface {
	// Forward an HTTP request through an agent tunnel session.
	// Returns the agent's HTTP response or an error if the session
	// is not found or the tunnel times out.
	ProxyRequest(ctx context.Context, in *TunnelProxyRequest, opts ...grpc.CallOption) (*TunnelProxyResponse, error)
}

type tunnelProxyClient struct {
	cc grpc.ClientConnInterface
}

func NewTunnelProxyClient(cc grpc.ClientConnInterface) TunnelProxyClient {
	return &tunnelProxyClient{cc}
}

func (c *tunnelProxyClient) ProxyRequest(ctx context.Context, in *TunnelProxyRequest, opts ...grpc.CallOption) (*TunnelProxyResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(TunnelProxyResponse)
	err := c.cc.Invoke(ctx, TunnelProxy_ProxyRequest_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// TunnelProxyServer is the server API for TunnelProxy service.
// All implementations must embed UnimplementedTunnelProxyServer
// for forward compatibility.
//
// TunnelProxy is an internal gRPC service that the stateless ControlPlane
// API calls to forward kubectl HTTP requests through a specific
// TunnelService pod that holds the agent's persistent tunnel stream.
//
// Flow:
//  1. ControlPlane receives a kubectl proxy request.
//  2. ControlPlane queries Redis for the tunnel pod that owns the
//     target cluster's session (e.g., "tunnel-0").
//  3. ControlPlane calls TunnelProxy.ProxyRequest on that pod's
//     internal gRPC endpoint (:50051).
//  4. TunnelProxy looks up the local TunnelSession, sends the HTTP
//     request frame to the Agent, waits for the response, returns it.
type TunnelProxyServer interface {
	// Forward an HTTP request through an agent tunnel session.
	// Returns the agent's HTTP response or an error if the session
	// is not found or the tunnel times out.
	ProxyRequest(context.Context, *TunnelProxyRequest) (*TunnelProxyResponse, error)
	mustEmbedUnimplementedTunnelProxyServer()
}

// UnimplementedTunnelProxyServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedTunnelProxyServer struct{}

func (UnimplementedTunnelProxyServer) ProxyRequest(context.Context, *TunnelProxyRequest) (*TunnelProxyResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ProxyRequest not implemented")
}
func (UnimplementedTunnelProxyServer) mustEmbedUnimplementedTunnelProxyServer() {}
func (UnimplementedTunnelProxyServer) testEmbeddedByValue()                     {}

// UnsafeTunnelProxyServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to TunnelProxyServer will
// result in compilation errors.
type UnsafeTunnelProxyServer interface {
	mustEmbedUnimplementedTunnelProxyServer()
}

func RegisterTunnelProxyServer(s grpc.ServiceRegistrar, srv TunnelProxyServer) {
	// If the following call panics, it indicates UnimplementedTunnelProxyServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&TunnelProxy_ServiceDesc, srv)
}

func _TunnelProxy_ProxyRequest_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(TunnelProxyRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(TunnelProxyServer).ProxyRequest(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: TunnelProxy_ProxyRequest_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(TunnelProxyServer).ProxyRequest(ctx, req.(*TunnelProxyRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// TunnelProxy_ServiceDesc is the grpc.ServiceDesc for TunnelProxy service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var TunnelProxy_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "clustral.internal.v1.TunnelProxy",
	HandlerType: (*TunnelProxyServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "ProxyRequest",
			Handler:    _TunnelProxy_ProxyRequest_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "tunnel_proxy.proto",
}
